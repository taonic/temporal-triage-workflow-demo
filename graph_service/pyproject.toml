[project]
name = "graph-service"
version = "0.1.0"
dependencies = [
    "flask>=2.3.3",
    "openai>=2.8.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
